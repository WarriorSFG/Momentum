import React from 'react';
import SelectionPage from './SelectionPage.jsx';

const PracticeS = () => {
  // This wrapper renders the selection page in 'normal' mode
  return <SelectionPage mode="normal" />;
};

export default PracticeS;